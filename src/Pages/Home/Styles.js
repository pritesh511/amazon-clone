import styled from "styled-components";

const HeroWrapper = styled.div``;
const HeroImage = styled.img`
  width: 100%;
  height: 100%;
`;
const ProductWrap = styled.div``;

export { HeroWrapper, HeroImage, ProductWrap };
